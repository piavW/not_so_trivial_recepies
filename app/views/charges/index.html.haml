%h1 Subscribe to become a premium member!
= form_with model: @charges, get: charges_path do
  - if flash[:alert].present?
    #error_explanation
      %p= flash[:alert]
    %label.amount
      %span
        Amount: 10000SEK
  %script.stripe-button{"data-amount" => "10000", 
  "data-description" => "A yearly subscription", 
  "data-key" => "#{Rails.configuration.stripe[:publishable_key]}", 
  "data-locale" => "auto", 
  :src => "https://checkout.stripe.com/checkout.js"}